[volumes]
data = "/data"

[cron.refresh-data]
schedule = "0 8,20 * * *"
command = "curl -s 'https://motivated-fulfillment-production-1d77.up.railway.app/api/cron/refresh-data?token=$CRON_SECRET'"
